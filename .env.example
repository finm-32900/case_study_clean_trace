WRDS_USERNAME="username"

# Debug date range (optional). When set, pull scripts only fetch this window.
# Useful for testing without pulling 20+ years of data.
# START_DATE=2024-01-01
# END_DATE=2024-02-28

# N_CORES=4  # Optional: cap parallel workers (auto-detects if not set)
